import "./Navbar.css"

const NavBar = () => {
    return (
        <nav>
            <a href="/">Home</a>
            <a href="/about">ABOUT</a>
            <a href="/login">Sign in</a>
            <a href="/register">Sign up</a>
        </nav>
    )
}

export default NavBar;